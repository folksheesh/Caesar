pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Print the username of the executing environment
                bat 'whoami'
            }
        }
    }
}
